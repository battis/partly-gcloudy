{
  "name": "@battis/partly-gcloudy",
  "version": "0.2.1",
  "description": "Idiosyncratic collection of interactions with `gcloud` CLI tool",
  "target": "node",
  "main": "./lib/esm/index.mjs",
  "types": "./lib/esm/index.d.ts",
  "type": "module",
  "engines": {
    "node": "*"
  },
  "author": {
    "name": "Seth Battis",
    "url": "https://github.com/battis"
  },
  "license": "GPL-3.0",
  "scripts": {
    "build": "run-s build:*",
    "build:clean": "shx rm -rf lib",
    "build:transpile": "tsc",
    "build:esm": "./bin/transform-node-to-esm-imports.cjs src lib/esm",
    "release": "run-s release:*",
    "release:build": "npm run build",
    "release:publish": "npx np --yolo --omit=dev"
  },
  "dependencies": {
    "@battis/qui-cli": "^0.1.7",
    "rword": "^3.2.1"
  },
  "devDependencies": {
    "@battis/eslint-config": "latest",
    "@battis/prettier-config": "latest",
    "@battis/typescript-tricks": "^0.1.0",
    "@tsconfig/recommended": "^1.0.2",
    "@types/node": "^20.4.1",
    "np": "latest",
    "npm-run-all": "^4.1.5",
    "shx": "^0.3.4",
    "typescript": "^5.1.6"
  },
  "prettier": "@battis/prettier-config",
  "eslintConfig": {
    "extends": "@battis/eslint-config"
  }
}
